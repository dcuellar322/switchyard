name: switchyard-compose-degraded-fixture

services:
  web:
    build:
      context: ../compose-healthy
    image: switchyard/compose-healthy-fixture:v1
    pull_policy: build
    healthcheck:
      test: ["CMD", "/server", "unhealthy"]
      interval: 1s
      timeout: 1s
      retries: 2
    ports:
      - "127.0.0.1::8080"
