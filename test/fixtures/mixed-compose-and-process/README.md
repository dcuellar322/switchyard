# Mixed Compose and process discovery fixture

The database is declared through Compose while the API and web development
commands are visible as deterministic uv and npm evidence. Discovery must not
execute any of them.
