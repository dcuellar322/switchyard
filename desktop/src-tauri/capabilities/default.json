{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local startup page access only; the remote application receives no Tauri capabilities.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
